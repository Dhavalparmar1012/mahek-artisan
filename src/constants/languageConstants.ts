export const FLAG_IMAGES: { [key: string]: string } = {
  en: "/images/flags/english-flag.png",
};

export const LANGUAGES = [{ title: "English", locale: "en" }];

export default LANGUAGES;

export enum LANGUAGES_TYPES {
  EN = "en",
}
